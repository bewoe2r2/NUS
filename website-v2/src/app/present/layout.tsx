/**
 * Minimal layout for presentation mode — intentionally bare (no nav/footer)
 * to provide a clean fullscreen presentation experience.
 */
export default function PresentLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
